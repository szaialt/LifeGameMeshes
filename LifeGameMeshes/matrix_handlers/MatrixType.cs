namespace matrix_handlers {
//Type to set beginning matrix
public enum MatrixType
    {
	RANDOM, //to generate random matrix
	ONE_POINT //to generate matrix from one living point
	//have sense only if a point can begin to live with 1 neighbour
    };
    
  }